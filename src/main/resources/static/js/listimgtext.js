$('#list_tabs a:first').click(function () {    window.location.href = '/web/listimgtext';});$('#list_tabs li:eq(1) a').click(function () {    window.location.href = '/web/listtext';});function getQueryString(activityId) {    var reg = new RegExp("(^|&)" + activityId + "=([^&]*)(&|$)", "i");    var r = window.location.search.substr(1).match(reg);    if (r != null) return unescape(r[2]);    return null;}window.onload = function () {			 onload = list(1);}var result;var totalpage;var list = function (event) {    $.ajax({        headers: {            Accept: "application/json; charset=utf-8"        },        type: "GET",        url: "http://vps1.taoware.com/message?type=news&offset=" + event + "&limit=5",        dataType: "json",        async: false,        success: function (data) {            result = data.list;            totalpage = data.totalpage;            $("table").html("<thead><tr><td>菜单名</td><td>开始/结束时间</td><td>图文标题</td><td>状态</td><td>操作</td></tr></thead>");            $("#pg_list").html("");            for (n = 1; n <= totalpage; n++) {                $("#pg_list").append(                    $("<li onclick='list(" + n + ")'><a>" + n + "</a></li>")                )            }            for (i = 0; i < data.list.length; i++) {            		var disable = data.list[i].disable                var imgtextId = data.list[i].id;                var imgtextmenuName = data.list[i].menuName;                var startDate = data.list[i].startDate;                var endDate = data.list[i].endDate;                var statustext = data.list[i].statusText;                var imgtexttitle = data.list[i].title;                var imgtextcount = data.list[i].count;                var imgtexturl = data.list[i].url;                var statusopen = '<a href="javascript:void(0);"  id="activityStatus" onclick="operate( ' + imgtextId +','+ event + ')">开启</a>';                var statusclose = '<a href="javascript:void(0);"  id="activityStatus" onclick="operate( ' + imgtextId + ','+ event +')">关闭</a>';                var statusinit = '';                if(statustext=="已结束"){                    statusinit = statusinit;                }else{                	  if (disable==true) {		                    statusinit = statusopen;		                }else{		                    statusinit = statusclose;		                }                	}                $("table").append(                    $("<tr class='tbody' id=" + imgtextId + "><td>" +                    imgtextmenuName                    + "</td><td>" +                    startDate + '&nbsp;' + '&nbsp;' + endDate                    + "</td><td>" +                    '<a href="' + imgtexturl + '" target="_blank" id="activityTitle">' + imgtexttitle + '</a>'                    + "</td><td>" +                    statustext                    + "</td><td>" +                    statusinit                    + "</td></tr>")                );            }            if (data.list.length < 5) {                for (i = 0; i < (5 - data.list.length); i++) {                    $("table").append("<tr class='tbody'><td height=35;></td><td></td><td></td><td></td><td></td></tr>");                }            }        }    });}var operate = function (event,event2) {    layer.config({        extend: ['skin/seaning/style.css'],        skin: 'layer-ext-seaning'    });    $("tr[id='" + event + "']").click(function () {        layer.confirm('确定操作？', {            btn: ['确认', '取消'], //按钮            shade: false //不显示遮罩        }, function () {            $.ajax({                url: 'http://vps1.taoware.com/message/' + event + '/disable',                type: 'GET',                success: function () {                    onload=list(event2);                                    }            });            layer.msg('已确定');        }, function () {            layer.msg('已取消', {shift: 0});        });    })}